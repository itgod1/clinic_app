<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clinic.mapper.MiniappUserMapper">

    <!-- 根据openid查询用户 -->
    <select id="selectByOpenid" resultType="com.clinic.entity.MiniappUser">
        SELECT * FROM miniapp_user WHERE openid = #{openid} LIMIT 1
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" resultType="com.clinic.entity.MiniappUser">
        SELECT * FROM miniapp_user WHERE phone = #{phone} LIMIT 1
    </select>

</mapper>
